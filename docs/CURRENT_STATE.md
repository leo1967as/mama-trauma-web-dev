## Status: active
## Last Updated: 2026-06-27

## Perf:
Bundle split: 1×936kB chunk → manualChunks (react/charts/motion/radix/vendor) + lazy MoodTab, CheckInFlow, EpdsFlow, SafetySection, secondary tabs, and MoodTrendChart. Initial JS ~164kB gz (was 269kB); recharts (308kB/80kB gz) now loads on demand, off the initial path. Verified headless (home/trend/mood/checkin OK).

## Confidence: high

## Priority:
Validate the new real-name / unassessed-state / explicit-empty-state workflow end to end with one fresh simulated mother.

## Next Action:
Create one brand-new mother in Afterbloom with legal name + preferred name + phone, leave staff-owned fields empty, then verify Admin shows "ยังไม่ประเมิน" and explicit missing-data states before triggering a flagged Check-in, EPDS Q10, or urgent Help path.

## Risks:
- Home intentionally keeps the Recharts-based mood trend, so the charts chunk still loads on the initial Home path.
- Lighthouse mobile throttled still shows LCP around 3.8s; the main remaining likely levers are font payload/subsetting and whether Vercel analytics scripts should run in prototype/local contexts.
- Impeccable live mode now loads only with `?impeccable_live`; live-variant workflows need that query param.
- Browser-level human QA is still recommended for the final fold feel, even though CDP interaction probes passed.
- LocalStorage keys were intentionally reset, so older app data will not carry over.
- Support Level and EPDS mapping are heuristic and may need tuning after browser validation.
- Caveman installer added repo-local `.agents/skills/` and `skills-lock.json`; decide whether these should be committed or ignored.
- Impeccable added repo-local skill artifacts under `.agents/skills/impeccable` and `.claude/skills/impeccable`; decide whether the `.claude` copy should be committed, ignored, or removed for this repo.
- `DESIGN.md` captures the current visual language; if the UI shifts materially, the file and `.impeccable/design.json` will need a refresh to avoid design drift.
- Reduced-motion handling is implemented and CDP-verified on Plans, but Therapy/booking/safety still deserves a manual OS/browser preference pass.
- Safety phone numbers remain prototype placeholders.
- Admin authentication is a client-side `admin/admin` localStorage flag and must never protect real patient data.
- Clinical scoring, alert wording, and emergency protocol remain drafts pending clinical advisor approval.
- Admin records staff actions as `Prototype Staff` until real staff identity is implemented with the deferred security work.
- Production Admin live-read verification passed through the public alias: 8 mothers and 8 actionable alerts loaded from Firebase with no browser console errors.
- Admin `npm run typecheck` still fails broadly because the existing JS/shadcn components lack usable TypeScript inference; lint and production build pass.
- Admin production bundle remains about 1.47 MB uncompressed and should be split after workflow validation.
- Mother App onboarding and Home settings now collect legal first/last name, preferred name, and phone, but this new schema still needs visible-browser QA through the full onboarding bridge and settings edit path.
- Admin now distinguishes `unassessed`, `none`, `manual`, and explicit missing-data labels, but the team still needs a fresh runtime tracer to confirm the live Firestore records behave correctly in the user's normal browser.

## Recent Context:
Implemented the planned state-model pass across Mother App and Admin. Mother App onboarding now collects legal first name, legal last name, phone number, and an optional preferred in-app name; Home settings can edit the same fields and sync them back to Firebase. New mother profiles now start as `assessmentState: unassessed` with `caseStatus: none` instead of being implicitly treated as low-risk. Mood support now exposes an explicit unassessed state instead of falling back to steady support before the first check-in. On the Admin side, mother normalization now derives legal/preferred display names, explicit missing-data labels (`ยังไม่เก็บจากคุณแม่`, `รอทีมดูแลกรอก`, `ยังไม่เก็บวันคลอด`, `ยังไม่คำนวณระยะ`), `assessmentState`, `caseStatus`, and `caseSource`. Mother Detail now keeps Check-in and EPDS cards visible with explanatory empty states, trend cards distinguish zero data from one data point, ActionPanel exposes `ยังไม่มีเคส` plus manual-case warnings, Alerts marks manual cases explicitly, and high-risk warning banners no longer appear just because staff manually changed case status. Mother App and Admin both pass lint and production build after the patch, with only the pre-existing Windows npm permission warning and bundle-size warnings remaining.
Normalized Admin mother profiles against the real prototype Mother App payload. `useFirestoreData` now derives safe display values from Firebase mothers: placeholder `Unknown` names are replaced with a Thai fallback, missing HN gets a prototype `AB-...` ID, missing avatars use a local SVG placeholder, and phone/support person/location/delivery fields are read defensively. Admin now derives `postpartumDay` from `deliveryDate` when needed and maps it into the Thai Journey Stage buckets expected by the current dashboard, fixing the mismatch where Mother App wrote `First Days` / `Early Adjustment` but Admin only understood `สัปดาห์ที่ ...`. Added a real onboarding event to the mother timeline so a newly created mother without check-ins no longer appears to have no history at all. Admin `npm run lint` and `npm run build` succeed after the patch, with only the pre-existing Windows npm permission warning and the existing large-bundle warning remaining.
Hardened `CheckInFlow` beyond the initial `.title` crash fix. Fixed the follow-up details crash by wiring the tiny-goal copy from `t.checkin.tinyGoal` instead of the nonexistent `t.checkin.result.tinyGoal`. Added real metadata for the computed `extra` support level so result messaging no longer falls back to `steady`. Switched the result "Next step" line to use i18n copy. Preloaded today's saved check-in values into edit mode so reopening the flow now restores the existing core answers and follow-up fields instead of starting from empty follow-up state. `npm run build` succeeds after the patch; `npm run lint` still exits successfully but prints the pre-existing Windows npm permission warning after completion.
Fixed the `CheckInFlow` follow-up crash that surfaced as `TypeError: Cannot read properties of undefined (reading 'title')` in the built chunk. Root cause: step 7 (`BabyConnectionScreen`) and step 8 (`SupportNeedScreen`) rendered `*.title` from props the parent never passed. Hardened the flow by passing the missing translation objects, normalizing localized question data before render, and removing the English-only `"Something else"` sentinel so Thai follow-up free-text persists correctly too. `npm run build` succeeds after the patch; `npm run lint` exits successfully but still prints the pre-existing Windows npm permission warning after completion.
Fixed production Firebase authentication failures. Added `afterbloom-app.vercel.app` to Firebase Authorized domains after confirming the Google provider was already enabled. Removed the disabled anonymous-signup dependency from app startup: restored Google users keep their Firebase UID, while guests receive a stable prototype device ID. Added a valid PWA manifest. Deployed production and verified the original failures no longer reproduce: manifest is valid JSON, no `accounts:signUp` request occurs, Google popup opens through the Firebase OAuth handler with the correct redirect domain, and there are no `unauthorized-domain`, `admin-restricted-operation`, or console errors.
Implemented the urgent prototype workflow gaps. Mother App Check-in, EPDS, and Safety sync now open reviewable cases with an alert reason and timestamp. Admin Mother Detail loads real check-in, EPDS, safety, and case-note subcollections; EPDS Q10 and urgent Help are explicit alerts; alert status persists through Firestore; resolved cases leave the active queue; and call, SMS, note, follow-up, referral, print, and CSV export actions are wired. The event timeline now uses real records instead of hard-coded examples. Both apps pass lint and production build and were deployed to `https://afterbloom-app.vercel.app` and `https://admin-dashboard-gamma-swart.vercel.app`. Production Chrome verification confirmed Admin login, 8 Firebase mothers, 8 actionable alerts, event timestamps, status controls, and no console errors. The final tracer check is the user's planned fresh mother simulation.
Fixed the final onboarding completion handoff so tapping "Go to my dashboard" now runs Progression as the only visible layer, switches to a full-opacity "Welcome to Afterbloom" moment, holds it before starting a 2s fade, then reveals Dashboard Home. Dashboard still remounts once when `onboarded` flips so Home consumes the fresh `justOnboarded` flag and updated display name. Verified `npm run lint`, `npm run build`, and a Playwright/system-Chrome smoke at 390px: Progression opacity stayed 1, Welcome opacity stayed 1 after a 1s hold, fade then began, and after completion `onboardingCount=0`, `statusCount=0`, `Leo.` visible, and no "You're ready" screen remained.
Fixed global help sheet layering by rendering the help overlay through a React portal to `document.body`, keeping it above Check-in/EPDS full-screen flows. Verified on production preview at 390px: help portal parent `BODY`, overlay z-index 110, EPDS flow z-index 100, and topmost element inside the help dialog. Ran Lighthouse mobile throttled on production preview after font cleanup: Performance 81, Accessibility 85, Best Practices 96, SEO 82, FCP 3.4s, LCP 3.8s, TBT 20ms, CLS 0.002, total size 410KiB, script requests 11. Delayed secondary tab chunk preloading removed eager tab chunk requests from the initial Lighthouse path while keeping first Mood switch around 80ms in the smoke test.
Ran `impeccable optimize` on the Afterbloom shell font path: removed render-blocking Google Fonts `@import`s from `src/index.css`, moved Newsreader/Plus Jakarta loading to `index.html` with `preconnect` plus one stylesheet request, made `--font-body` the Tailwind body font, and kept `font-nunito` as a compatibility alias. Verified `npm run lint`, `npm run build`, and dev endpoint `http://127.0.0.1:5173/` returning 200 with font preconnect present and no Nunito reference in the served HTML.
Implemented the tab-switch jank fix: gated Impeccable live script behind `?impeccable_live`, preloaded lazy tabs, changed tab navigation to wait for lazy chunks before switching, removed Dashboard tab skeleton fallback, unified Dashboard tab shell padding, and removed per-tab root entrance motion from Mood/Therapy/Plans/Circle. Verified with `npm run lint`, `npm run build`, and Playwright-driven Chrome at 390px: skeleton frames 0 for Mood/Therapy/Plans/Circle/Home, live overlay nodes 0, and no measured layout shifts during repeated tab switches.
Diagnosed tab-switch jank with a real Vite dev server and Playwright-driven Chrome at 390px width. Root causes are stacked transitions in `Dashboard.jsx` plus per-tab entrance motion, first-load lazy tab skeleton swaps, inconsistent tab shell padding, and the dev-only Impeccable live script in `index.html`.
Rebranded MaMa to Afterbloom across source paths, storage keys, user-facing copy, and mock data; renamed `src/components/calmmama` to `src/components/afterbloom`; switched Risk Level to Support Level; wired a global I Need Help sheet from Dashboard; and verified the app with build and lint.
Tooling-only update: installed JuliusBrussee/caveman skills for this repo and synced them into global Codex skills.
Follow-up tooling check: verified `caveman` exists in both `.agents/skills/` and `C:\Users\LEO\.codex\skills`, and this session exposes the caveman skill family; if it "doesn't show up", the likely cause is using a session started before the install/sync completed.
Installed `impeccable` via `npx impeccable skills install --yes` (with a temporary Windows `unzip` shim because the CLI assumes `unzip` exists in PATH). Verified `impeccable` now exists under `.agents/skills/` and `.claude/skills/`, and `npx impeccable detect src\pages\tabs\HomeTab.jsx` exits successfully.
Completed the first `impeccable init` outcome in-repo: added root `PRODUCT.md`, root `DESIGN.md`, `.impeccable/design.json`, and `.impeccable/live/config.json` tailored to Afterbloom as a warm, private, steady postpartum product UI with explicit anti-period-tracker and WCAG AA/reduced-motion guardrails.
Therapy UX pass: changed the tab into a booking-first support surface, removed the confusing "Example session" primary state, added a no-session empty state, softened therapist cards toward care fit, refined booking copy, and split `Ask my care team` from urgent `I don't feel safe right now` behavior.
UX UI Pro Max audit follow-up: used the existing local skill guidance to harden booking/safety interactions, added dialog semantics and live progress status, raised calendar/time/safety CTA targets to 44px, removed the unused mock upcoming session export, and verified Therapy/no-session/safety/booking-confirm flows through Chrome headless CDP.
Plans UX UI Pro Max follow-up: hardened care-plan generation, accordion, checklist, and progress interactions; added live loading/status, progressbar semantics, expanded/checked states, reduced-motion-safe loading, and verified generate/regenerate/checklist flows through Chrome headless CDP at 390px.
BottomNav safe-area pass: added shared `LAYOUT` and `LAYERS` tokens, reserved bottom navigation space in Dashboard, hid Dashboard-owned nav during Check-in/EPDS/help overlays, raised nested BookingFlow above nav, and verified Booking/Check-in/EPDS Continue buttons remain tappable at 360px width.
Check-in result scroll fix: constrained the CheckInFlow flex chain with `minHeight: 0`, hidden overflow wrappers, and touch-friendly scroll panes so the result screen scrolls on 360x640 instead of expanding below the viewport.
Mood polish pass: refined MoodTab copy/hierarchy, added semantic sections/list/chart labeling, moved the trend chart to the shared lazy chart component with reduced-motion behavior, raised visible interactive targets to 44px+, added focus styling, and verified steady/immediate support actions through Chrome CDP.
AI-slop audit follow-up: current UI is not pure AI slop, but still has visible wellness-template residue from repeated blurred gradient blobs, uppercase tracked section labels, white rounded cards with hairline borders plus soft shadows, rounded icon tiles, and a glassy floating bottom nav.
